function projectsCreation(input){
    let name = input[0];
    let projects = input[1];
    let hours = (3);
console.log(`The architect ${name} will need ${projects*hours} hours to complete ${projects} project/s.`)
    
}

projectsCreation(["George",4,3]);